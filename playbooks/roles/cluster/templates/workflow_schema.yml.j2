---
- job_template: "{{ tower_job_template_authenticate_tower_name }}"
  success:
  - job_template: "{{ cluster_job_template_preflight_name }}"
    success:
    - job_template: "{{ cluster_job_template_generate_inventory_name }}"
      success:
      - job_template: "{{ cluster_job_template_prerequisites_name }}"
        success:
        - job_template: "{{ cluster_job_template_install_name }}"
          success:
          - job_template: "{{ cluster_job_template_post_install_name }}"
          failure:
          - job_template: "{{ cluster_job_template_install_name }}"
            success:
             - job_template: "{{ cluster_job_template_post_install_name }}"
